export interface Article {
	id: number;
	title: string;
	slug: string;
}

export interface Category {
	id: number;
	name: string;
	slug: string;
}

export interface Tag {
	id: number;
	label: string;
}
